# Copy this file to  backend/.env.local  (same folder).
# Lines here override backend/.env  — gitignored so your laptop prefs never commit.

# Turns off RDS/PostgreSQL for local runs; DATABASE_URL in .env is ignored.
ARBITER_LOCAL_DEV=true
